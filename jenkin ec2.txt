pipeline {
    agent any

    stages {
        stage('Print Hello World on EC2') {
            steps {
                sshagent(['ec2-ssh-key']) {
                    sh '''
                        ssh -o StrictHostKeyChecking=no ec2-user@<35.175.194.22> "echo Hello World"
                    '''
                }
            }
        }
    }
}
